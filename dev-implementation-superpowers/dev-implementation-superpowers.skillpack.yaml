name: dev-implementation-superpowers
description: "Disciplined subagent-driven implementation workflow: git preparation, brainstorming, spec-first planning, supervised dispatch with two-stage review, and controlled branch completion."
version: "0.1.0"
includes:
  # Domain contract
  - skill:subagent-dispatch-contract
  # Setup
  - skill:git-prepare
  # Pre-implementation
  - skill:brainstorm
  - skill:write-plan
  # Supervised implementation
  - skill:implement-supervised
  # Branch completion
  - skill:finish-branch
