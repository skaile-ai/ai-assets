name: dev-quality
description: "Quality assurance pipeline — code audit, test plan, unit tests, integration tests, E2E tests, readiness gate, and cross-reference sync."
version: "0.1.0"
includes:
  # Contract (required by all skills in this domain)
  - skill:quality-contract
  # Audit
  - skill:audit
  # Testing
  - skill:test-plan
  - skill:test-unit
  - skill:test-integration
  - skill:e2e
  # Gates and utilities
  - skill:ready
  - skill:compile-validators
  - skill:sync
  # Domain agent
  - agent:quality
