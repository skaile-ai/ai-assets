# cf_concept_functionality_behaviors CLI

## Trigger

Invoke with: "Formalize the behavior" or "Write behavioral specs" or "Create allium specs from features"

## Prerequisites

- `_concept/03_features/**/*.md` must exist (run `cf_functionality_features` first)

## Output

- `_concept/03b_behavior/<group_name>.allium` — one behavioral spec per feature group

## Next Step

After human approval → define brand (`cf_concept_brand_visual`), tech stack (`cf_concept_techstack`), then run `cf_concept_datamodel`
