version: "1.0"
description: Forge Project assistant knowledge base — injected at project open

documents: []

# Project-specific context comes from the project's own CLAUDE.md (if present).
# This knowledge base is intentionally minimal — the assistant knows how to code,
# not the internals of the platform it runs on.

retrieval:
  strategy: load_all
