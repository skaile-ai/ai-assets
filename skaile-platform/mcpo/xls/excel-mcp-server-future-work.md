# Excel MCP Server — Future Work

> Living document. Seeded from §14 of `excel-mcp-server-implementation-plan.md` at project start (per §1.6). Every "we'll handle this later" judgement call made during implementation gets appended under "Plan deferrals during implementation" below.

---

## 1. Format & engine

- **`.xlsb` read or write.** Rejected at open with future-friendly error. Future option A: calamine sidecar binary for read. Future option B: LibreOffice convert-on-open + write back as `.xlsx`.
- **Streaming engine** for very large workbooks (>100 MB / >1M cells). Future: second `WorkbookEngine` impl using SXSSF (write) + XSSF SAX EventModel (read), gated by a per-workbook size heuristic.
- **LibreOffice / UNO bridge** for chart rendering, PDF/image export, and full-fidelity recalc on functions POI's evaluator can't handle. Future: ship as a sidecar process, MCP shells out per call.
- **Aspose.Cells fallback** for fidelity gaps neither POI nor LibreOffice cover. Commercial license — only pursue if a real need surfaces.

### 1a. Formula coverage gap (the biggest known limitation)

POI's `FormulaEvaluator` implements ~280 of Excel's ~500+ functions. Everything Microsoft has added since Excel 2019 is missing. v1 strategy: **detect via `workbook.capabilities_report` and rely on recalc-on-open** (Excel/LibreOffice fixes stale values when a human opens the file). This is fine for interactive flows; it breaks if the workbook is consumed headlessly by another system.

**Concretely missing:**

- Dynamic arrays: FILTER, SORT, SORTBY, UNIQUE, SEQUENCE, RANDARRAY
- LAMBDA family: LAMBDA, LET, BYROW, BYCOL, MAP, REDUCE, SCAN, MAKEARRAY
- Array shaping: VSTACK, HSTACK, TOCOL, TOROW, WRAPCOLS, WRAPROWS, CHOOSECOLS, CHOOSEROWS, TAKE, DROP, EXPAND
- New text: TEXTSPLIT, TEXTBEFORE, TEXTAFTER, REGEX family
- New pivot: GROUPBY, PIVOTBY, PERCENTOF
- IMAGE() (2023), PY() (2024), TRIMRANGE (2024)

**Structural gaps:**

- **Dynamic-array spill semantics.** POI handles legacy CSE array formulas (`{=...}`) but not the modern dynamic-array model where one formula spills into adjacent ghost cells. POI reads cached spilled values but cannot recompute them.
- **`@` implicit intersection** and **`#` spilled range reference** — parsed literally, not applied semantically.

**Future options to close the gap, in priority order:**

1. **HyperFormula sidecar** (MIT, JS, 400+ functions including dynamic arrays). Run in Node, communicate over a Unix socket. ~2–3 weeks integration. Best cost/benefit for v1.1 or v2.
2. **Univer sidecar** (Apache 2.0, JS, full dynamic array + some LDT support). Alternative to HyperFormula.
3. **LibreOffice headless** — slower per call, heavier image, but covers more than HyperFormula.
4. **Hybrid cascade** — POI native → HyperFormula → LibreOffice fallback.
5. **Microsoft Graph / Excel Online** — 100% parity but requires cloud and data egress. Document as the "if all else fails" option.

### 1b. Linked Data Types (Stocks, Geography, custom Power Query types)

POI has **zero support**. The data lives in OPC parts under `/xl/richData/*` referencing `/xl/metadata.xml`. POI preserves the parts as opaque blobs on round-trip but cannot introspect or modify them. Older viewers see `#VALUE!`.

v1 strategy: **detect via `workbook.capabilities_report` and warn**. Don't attempt to read or modify LDT cells. Don't strip them.

**Future options:**

1. **Detect + flag** (current): capabilities report says "this workbook has LDT cells; preserve only, do not modify."
2. **Strip and degrade** to the fallback string value, drop the rich data parts. Destructive — only suitable if user explicitly opts in.
3. **Microsoft Graph / Excel Online** — only realistic option for actually round-tripping LDTs.

## 2. Workbook content not in v1

- **Charts**: read structural info, render to PNG, create. (Render needs LibreOffice; structural read/create is POI but verbose.)
- **Pivot tables**: read structural info, refresh, create. (POI's pivot support is very limited — needs LibreOffice for anything non-trivial.)
- **Cell formatting writes** beyond the read-side `include_formatting=true` exposure: font/color/border/fill/conditional formatting.
- **Data validation rules** (read or write).
- **Copy operations**: `range.copy`, `sheet.copy` (cross-workbook too). xlport's `copyCell` style-map pattern is a useful reference.
- **Merge / unmerge cells** (v1 `sheet.merged_regions` is read-only).
- **Hyperlinks**, **comments**, **threaded comments**, **images / drawings / shapes**.
- **External workbook links** read/repair.
- **Sheet protection** (lock cells, protect with password). xlport's `Template.protectWorkbook` is a reference.
- **Page setup / print area / headers & footers**.

## 3. Power Query & Data Model

- **Power Query (M) extraction**: subprocess to PowerQueryViewer. Adds Python to image.
- **DAX / PowerPivot model introspection**: subprocess to PBIXRay. Adds Python + apsw deps.
- **Power Query / DAX writing or modification**: not realistically possible without Excel itself. Defer indefinitely.

## 4. VBA beyond read

- **VBA execution**: requires real Excel in a sandbox. Defer indefinitely.
- **VBA module editing / writing**: technically possible (rebuild `vbaProject.bin`) but extremely fiddly. Defer until there's a real need.
- **UserForm extraction**: known-incomplete in every Java/Python tool. Document the limit if/when first asked.

## 5. Transport & deployment

- **HTTP transport** (streamable HTTP, SSE) — architecture supports it; v1 ships stdio only.
- **Multi-tenant shared server** — would require per-handle locking, idle-handle eviction, optional per-session sandboxing.
- **Auth on the MCP transport itself** — currently delegated to whoever owns the container.
- **Metrics endpoint** (Prometheus / OpenTelemetry).
- **Sibling MCPs** (Word via XWPF, PowerPoint via XSLF) — same architecture, separate projects, shared utility lib.

## 6. Test & ops nice-to-haves

- **Larger fixture corpus** (subset of `xlport-internal/src/test/resources/test-suites/` if approved).
- **Performance regression tests** on large workbooks.
- **ArchUnit** tests enforcing the §4.1 layer rules.
- **Per-workbook auto-save policy** option (currently we never auto-save).
- **Soak / leak tests** for handle registry under long sessions.
- **JSON log format** option (alongside plain-text) for centralized log pipelines.
- **Parser-based function-name extraction in `workbook.capabilities_report`.** v1 uses the cheap regex path per plan §9.1 ("pick the cheap path for v1"), which matches `FUNCTION(` patterns inside string constants of formula cells — so a formula like `="see IF( here"` produces a false-positive `IF` in `functions_used`. Confirmed in manual testing post-Phase-7 (FILTER-fixture probe with a string-constant decoy formula). The false positives only pollute `functions_used`; `unsupported_functions_used` remains accurate because it is cross-referenced against `FunctionEval.getSupportedFunctionNames()`. Future better: switch to `FormulaParser.parse(formula, fpb, FormulaType.CELL, sheetIndex)` and walk the returned `Ptg[]` for `FuncPtg` / `FuncVarPtg` instances; zero false positives, more code. Low priority — the critical agent signals are already correct.

---

## Authoring conventions (standing directives)

These apply to every phase from this point forward, across sessions. Follow them when writing new code; if you inherit code that violates them, fix it.

### Tool description template — required for every new tool from Phase 5 onward

The `ToolDefinition.description()` returned by each tool must be a single block of text with **three parts, in this order**:

1. **Effect statement** — one sentence describing what changes in the world when the tool runs (e.g. "Loads an Excel workbook into memory and returns an opaque handle for subsequent calls.").
2. **Prerequisite / lifecycle note** — what must have run before and/or what this tool enables after (e.g. "Requires a handle previously returned by `workbook.open` or `workbook.create`; the returned handle stays valid until `workbook.close` or process exit.").
3. **One important gotcha or constraint** the LLM needs in order to use the tool correctly (e.g. "Changes are in-memory until `workbook.save`.", "Empty cells return `type:blank`, not an error.", "Formula cells do not auto-evaluate — call `workbook.recalculate` to refresh cached values.").

Keep each part one sentence. The whole description should read in under ~60 words.

### Parameter description rules

Every `properties.<name>.description` in a tool's input schema must state:

- **Valid value forms** with at least one concrete example (e.g. `"A1 range like \"A1:C10\" or a single-cell form like \"C5\""`).
- **Constraints** (length, domain, allowed enum values).
- **Units** where applicable (bytes, row indices 1-based vs 0-based, UTC vs naive datetime).

Do **not** duplicate what the JSON Schema already encodes (`type`, `required`, `default`). Do **not** mention POI, XSSF, or any engine-layer concept — agents see the tool surface only.

---

## Plan deferrals during implementation

Appended as decisions are made. Each entry: what was chosen, why, and a one-line sketch of the future-better option if one came to mind. Look for matching `// PLAN-DEFER:` comments in code.

> 🛑 **STOP-GATES for future implementation sessions.** Before acting on any of these, pause and ask the human to choose. Do NOT decide unilaterally — these interact with constraints outside the plan.
>
> - ~~**Phase 10, Dockerfile user:** pick one of the three options in "Container UID vs. bind-mount ownership" below before touching the Dockerfile.~~ **Resolved Phase 10** — Option 2 chosen (change default UID to 1000). See the deferral entry below.
> - **Phase 10, tool-description audit:** review all 26 tool `description()` strings and their parameter descriptions in a single editorial pass against the "Tool description template" and "Parameter description rules" above. Fix any that don't conform (Phases 0–4 tools predate the template; `workbook.list_handles` is authored fresh in Phase 10 and should conform from the start). Roughly a 30-minute pass over ~260 lines total. Do this as polish, not ad-hoc during earlier phases.

> 🛠 **Phase 10 concrete tasks (no human input needed; just do them):**
>
> - ~~**Commit a permanent minimal `.xlsm` fixture** at `src/test/resources/fixtures/vba-hello.xlsm`…~~ **Done Phase 10.** Fixture committed at `src/test/resources/fixtures/vba-hello.xlsm` (6.3 KB) with provenance recorded in `src/test/resources/fixtures/README.md`; `smoke/phase9-smoke.sh` now uses the committed fixture unconditionally (no env-var, no host-path fallback).

### Open items to discuss at the end of v1

- **`PATH_OUTSIDE_ROOT` fix: reproducibility disagreement between implementer and operator.** After the §6.1 reorder landed (see "Fixed: `PATH_OUTSIDE_ROOT` was unreachable…" below), end-to-end verification inside a fresh `docker build --no-cache -t excel-mcp:dev .` image returned the expected `PATH_OUTSIDE_ROOT` for `/tmp/foo.xlsx`, `/data/../etc/passwd`, and `/etc/os-release` — and `FILE_NOT_FOUND` for `/data/doesntexist.xlsx` — with server startup confirming `EXCEL_MCP_ROOT=/data (path sandboxing enabled)`. The human still reported seeing `FILE_NOT_FOUND` for `/tmp/foo.xlsx` under the MCP Inspector against what they believed was a fresh image. Most likely: stale docker layer, inspector UI caching a prior response, or a dual docker daemon (WSL/Desktop) showing different images to different shells. **To revisit at end of v1:** reproduce together, capture the exact inspector response payload + server stderr, confirm which daemon / image hash is actually serving the MCP call, and close out one way or the other. No code change to attempt until that diagnosis.

- **Jackson version: 2.20.1 instead of the plan's 2.18.x.** (Phase 0, pom.xml) The MCP Java SDK `mcp-json-jackson2:1.1.1` transitively pulls `jackson-databind:2.20.1`; pinning 2.18.x would force a dual-version shade and surprise null/date handling. Chose SDK alignment. Future better: watch for an SDK drop that realigns and re-pin to whatever the team standardises on.
- **Resolved (Phase 10): Container UID vs. bind-mount ownership — Option 2 chosen (Dockerfile default UID is now 1000).** (Observed Phase 4, WSL dev host; resolved Phase 10) The `Dockerfile` previously ran the app as `USER 10001`, which could not write to host-owned bind-mounted files (host UID typically 1000) unless the operator overrode with `--user`. Phase 10 switched the default to UID 1000 (`useradd --uid 1000`, `USER 1000`) so the overwhelming majority of Linux/WSL dev and CI hosts don't need `--user` at all. README now includes a "Running with a different UID" subsection explaining why UID 1000 is the default, showing the `--user "$(id -u):$(id -g)"` override for hosts whose primary UID differs, and noting that Mac Docker Desktop and Kubernetes (`securityContext.runAsUser`) typically don't require the override. Options not taken: (1) documentation-only — rejected because developer ergonomics matter on first run; (3) gosu/su-exec entrypoint with runtime UID detection — rejected because the brief root phase breaks Kubernetes `runAsNonRoot: true` and many enterprise baselines, and the added shell script / binary dependency wasn't justified given Option 2's heuristic works for ~all observed hosts. Future reverse-point: if a host or deployment surfaces where UID 1000 is the wrong default (e.g. a managed CI runner assigning unconventional UIDs and not passing `--user`), revisit Option 3 or a compile-time UID arg.

- **Fixed: `A1:XFE1` returned blanks instead of `RANGE_OUT_OF_BOUNDS`.** (Phase 4 manual testing) `readRange`/`writeRange`/`clearRange` iterated the requested address blindly, so any range whose end column or row exceeded the workbook's hard format limits (xlsx: XFD / 1,048,576; xls: IV / 65,536) silently returned empty cells. Fixed by asserting against `SpreadsheetVersion.getLastRowIndex()` / `getLastColumnIndex()` in all three range ops before iteration. Regression suite: `RangeBoundsTest` (5 cases, incl. XFE1 on reads/writes/clears and the accept-at-XFD boundary). **Chose to NOT enforce the §9.2 "data-extent" variant** ("Range D1:D10 is outside sheet 'Sheet1' (max column C, max row 5)") — it's noisy for legitimate "show me A1:Z100 on this small sheet" calls, interacts poorly with cleared cells (after `range.clear`, `getLastCellNum()` may return -1 and honest reads like A1:B2 would then fail), and is O(rows) to compute the max column. Format bounds alone address the reported bug. Future better: an opt-in `strict_bounds=true` parameter on `range.get` for callers who do want the data-extent check.
- **Fixed: `PATH_OUTSIDE_ROOT` was unreachable (security — must-fix class, caught pre-production).** (Phase 4 manual testing) In the original `PathValidator`, file-existence and (elsewhere) the format-whitelist ran before the `EXCEL_MCP_ROOT` containment check, so every path outside the sandbox surfaced as `FILE_NOT_FOUND` or `FORMAT_UNSUPPORTED` and the sandbox signal was dead code. Reproduced with `EXCEL_MCP_ROOT=/data`: `/tmp/foo.xlsx → FILE_NOT_FOUND`, `/data/../etc/passwd → FORMAT_UNSUPPORTED`, `/etc/os-release → FORMAT_UNSUPPORTED` — all should have been `PATH_OUTSIDE_ROOT`. Fixed by reordering to §6.1: parse → containment → format → existence, and replacing `allowParent` with a single best-effort canonicaliser that uses `parent.toRealPath()` when the target is missing so symlink escapes (`/data/link -> /etc/passwd`) are caught even for destination paths. Added 8 regression tests in `PathValidatorTest`. Severity: **security — must fix before production.** Impact for local dev was low (no sandboxing without `EXCEL_MCP_ROOT`).
- **`workbook.create` seeds a default "Sheet1".** (Phase 4 manual testing) A brand-new `XSSFWorkbook` has zero sheets and Excel rejects the saved file as corrupt. Since `sheet.create` isn't available until Phase 6, `workbook.create` + `workbook.save` would have been unusable end-to-end. Fixed by calling `workbook.createSheet("Sheet1")` immediately after `new XSSFWorkbook()` in `XssfInMemoryEngine.create`, matching Excel's "New Workbook" default. Plan §9.1 updated inline. Future consideration: once `sheet.create` exists (Phase 6), an optional `initial_sheet` arg on `workbook.create` could let the caller pick the seed sheet's name.
- **Case-insensitive sheet-name lookup.** (Phase 2, `XssfInMemoryEngine.requireSheet`) The plan does not specify whether sheet lookups are case-sensitive. Excel itself treats sheet names as case-insensitive in practice. Chose: exact match first, case-insensitive fallback. Future better: if a user reports ambiguous-match surprises, add a strict-mode toggle via env var.
- **`workbook.recalculate` tolerates `NotImplementedException` per cell instead of failing the whole recalc.** (Phase 5, `PoiFormulaEvaluation`) The plan said "on POI throw, raise `FORMULA_EVAL_FAILED` with the cell address." Literal reading: *any* POI exception, including the expected `NotImplementedException` POI raises for FILTER / LAMBDA / dynamic-array functions it doesn't implement. That would make the tool unusable on any modern workbook. Chose: catch `NotImplementedException` per cell and skip (leaving the cached value alone), rethrow every other `RuntimeException` as `FORMULA_EVAL_FAILED` with the cell address. The skipped cells are the ones `workbook.capabilities_report` already surfaces as `unsupported_functions_used`, so the agent has the information to reason about staleness. Future better: return the skip count in the tool response (currently only successful evaluations are counted).
- **Per-workbook `FormulaEvaluator` caching lives on the engine map, not on `OpenWorkbook`.** (Phase 5, `XssfInMemoryEngine`) Plan §5.2 sketch put the evaluator on the `OpenWorkbook` record; that leaks POI types into `handles/` and violates the §4.1 / §11b.5 boundary (same reasoning as the original `OpenWorkbook` deviation). Chose: hold `Map<HandleId, FormulaEvaluator> evaluators` inside `XssfInMemoryEngine`, mirror the existing `workbooks` map, clean up both on close. Future better: wrap the POI workbook + evaluator + registry metadata into an engine-internal `OpenWorkbookState` record so the per-handle lifecycle is a single thing to manage.
- **Dynamic-array detection uses `CTCell.isSetCm()`, not `CTCellFormula.getCm()`.** (Phase 5, `PoiCapabilityScanner`) The plan §9.1 text suggested walking `<f cm="1">` attributes, but the `cm` attribute is actually on the parent `<c>` element in the OOXML schema — and the `poi-ooxml-lite` build omits `cm` from `CTCellFormula` entirely, only `CTCell.isSetCm()` is exposed. Chose: cast `cell` to `XSSFCell`, read `getCTCell().isSetCm()`. Works on XSSF workbooks; HSSF is always `false` (dynamic arrays post-date the binary format). Minor spec drift, no behavioural gap.
- **Unsupported-function detection uses the regex path from §9.1.** (Phase 5, `PoiCapabilityScanner`) Plan documented two options — regex over `cell.getCellFormula()` (false positives for identifier-shaped substrings inside string constants) vs. `FormulaParser.parse` walking `Ptg[]` (correct but much more code). Plan says "pick the cheap path for v1". Done. `_xlfn.` / `_xlws.` prefixes are stripped before the supported-set lookup so POI-normalised future-function names match properly. Future better: swap in the FormulaParser/`FuncPtg` walk if false positives ever bite.
- **`has_vba` heuristic includes `.xlsm` files without an inline `vbaProject.bin` part.** (Phase 5, `PoiCapabilityScanner`) POI's `XSSFWorkbook.isMacroEnabled()` returns true for anything loaded from a `.xlsm`; we OR that with the OPC-part check for `/xl/vbaProject.bin`. Over-inclusive on workbooks that were saved as `.xlsm` but have no actual macros, which is harmless for a "does this need VBA tooling?" signal. Future better: look at `vbaProject.bin` size / structure to distinguish empty-VBA from real VBA, once `vba.list_modules` lands in Phase 9.
- **No `SHEET_NAME_INVALID` error code.** (Phase 6, `XssfInMemoryEngine.createSheet`/`renameSheet`) POI's `IllegalArgumentException` for invalid sheet names (length > 31, forbidden chars `: \ / ? * [ ]`, leading/trailing apostrophe) is surfaced as `INTERNAL_ERROR` with POI's message in `details`. Plan §8.2 defined a fixed error-code set that didn't include a name-validity code, and the duplicate-name path (the common case) is already covered by `SHEET_ALREADY_EXISTS` from a pre-check. Future better: extend the enum with `SHEET_NAME_INVALID` and validate up front (regex + length) rather than letting POI throw.
- **Column-shifting on HSSF (.xls) surfaces as `INTERNAL_ERROR`.** (Phase 7, `sheet.insert_cols`/`delete_cols`) POI's `HSSFSheet` does not implement `shiftColumns`; calls throw `UnsupportedOperationException` which the dispatcher converts to `INTERNAL_ERROR`. Row-shifting works on HSSF, so the parity gap is columns only. Tool descriptions flag the limitation explicitly. Future better: detect the HSSF case up front and return a dedicated code (`FORMAT_UNSUPPORTED` with a specific sub-reason, or a new `OPERATION_NOT_AVAILABLE_FOR_FORMAT` code) so the agent sees a first-class signal; or implement column shifting for HSSF ourselves using the POI low-level row/cell APIs.
- **Row/column operations do not automatically recalculate dependent formulas.** (Phase 7, all four tools) POI rewrites cell references when rows shift but does NOT re-run the formula evaluator, so cached results on dependent cells go stale. The tool descriptions for `sheet.delete_rows`/`delete_cols` tell the agent to call `workbook.recalculate` afterwards. Future better: an optional `auto_recalc: true` flag on the mutation tools, or an engine-level policy that triggers recalc on every structural change.
- **`shiftColumns` defensive guard compares against `computeLastFilledColumn` scan.** (Phase 7, `XssfInMemoryEngine.insertCols`/`deleteCols`) The plan §9.4 footgun check uses a per-call O(rows) scan to find the last column with data. Fine for typical workbooks but not cheap on wide sheets with millions of cells. Future better: cache the per-sheet "max filled column" on the engine side and invalidate on writes / clears / shifts.
- **Fixed: `range.set` → `range.get` pre-recalc returned `{type:"number", value:0}` instead of `{type:"formula_uncomputed", value:null}` (plan §7.1 violation).** (Phase 5 follow-up) POI's `setCellFormula` leaves the `t` attribute defaulting to `"n"` (NUMERIC), so `getCachedFormulaResultType()` reports NUMERIC and `getNumericCellValue()` falls back to `0.0` when `<v>` is absent. The cached-type hint cannot be trusted as the "has a cached value" signal on its own. Fix is two-sided: `PoiCellWriter` unsets the `<v>` element on XSSF cells after `setCellFormula` as defense against stale cached values from prior writes on the same cell; `PoiCellReader` now treats `!isSetV()` on an XSSFCell with FORMULA type as the authoritative "uncomputed" signal and returns `formula_uncomputed` directly, bypassing the cached-type hint. Regression assertion added to `smoke/phase5-smoke.sh`: after writing `=SUM(A1:A3)` and before `workbook.recalculate`, A4 must read as `{type:"formula_uncomputed", value:null}`.
- **Fixed: unlinked external reference in a freshly-written formula surfaced as `INTERNAL_ERROR` instead of `FORMULA_INVALID`.** (`PoiCellWriter.write`, post-Phase-7 follow-up) `range.set` with a formula like `=[NichtExistent.xlsx]Sheet1!A1` triggered POI's `IllegalStateException("Book not linked for filename NichtExistent.xlsx")` from inside `setCellFormula`. The previous narrow catch only handled `FormulaParseException`, so this fell through to the dispatcher's catch-all and surfaced as `INTERNAL_ERROR` — misleading: per plan §8.2, `FORMULA_INVALID = "cannot be parsed by POI's formula parser"`, and an unlinked external ref is a parser-stage rejection. Fix: widen the catch in `PoiCellWriter.write` to `catch (FormulaParseException | IllegalStateException e)` and map both to `FORMULA_INVALID` with POI's message passed through and the exception class name added to `details`. Important distinction: `evaluator.setIgnoreMissingWorkbooks(true)` only helps on the recalc path for formulas already stored in the workbook (loaded from disk); it does nothing for fresh `setCellFormula` calls on unlinked refs, which must be rejected at write time. Regression: `smoke/phase-regression-unlinked-external-ref-is-formula-invalid.sh` asserts `code == "FORMULA_INVALID"` for both the external-ref case and the classic truncated-formula syntax case.
- **Fixed: `workbook.close` silently overwrote external edits made between open and close — data-loss bug (must-fix severity, caught post-Phase-7).** (`XssfInMemoryEngine.open`) `WorkbookFactory.create(path.toFile(), null, readOnly=false)` gave POI a read/write `OPCPackage` handle on the source file. When the workbook was closed (`workbook.close` or JVM shutdown hook), POI's `OPCPackage.close()` silently flushed the in-memory package state back to the original path — even if no `workbook.save` had been called. Reproduced sequence: MCP `workbook.open` → external writer opens the same file, mutates A1, saves, closes → MCP `workbook.close` → file on disk reverts to the pre-external-edit state (or throws `OpenXML4JRuntimeException: Fail to save` when the external mutation shifted package structure). The `readOnly=true` alternative blocks `wb.write(OutputStream)` too (`OPCPackage.save` throws on a read-only package), so save would stop working. Fix: load via `Files.newInputStream(path)` → `WorkbookFactory.create(InputStream)` so POI reads the bytes into memory at open time and never holds a file-backed package; `PoiAtomicSaver` continues to work unchanged (uses `wb.write(OutputStream)` + temp/rename, which goes through `OPCPackage.save(OutputStream)` and is allowed on an in-memory package). Trade-off: peak memory during load is roughly 2x the file size — acceptable within the 100 MB `EXCEL_MCP_MAX_FILE_BYTES` cap. Regression smoke `smoke/phase-regression-close-does-not-overwrite-external-edits.sh` reproduces the exact sequence and asserts md5 + semantic equality against the external edit after MCP close. Future better: add a per-open "read-back from disk on close" audit that diffs against what we wrote via save (catches silent overwrites from any future POI regression).
- **§5.2 `OpenWorkbook` record shape.** (Phase 1, `handles/OpenWorkbook.java`) The plan's example had `OpenWorkbook` hold an `XSSFWorkbook` + `FormulaEvaluator` directly. That leaks POI types into `handles/` and transitively into `tools/`, violating §4.1 layer rules and §11b.5 "POI types leaking into the tool layer or response shapes". Chose: `OpenWorkbook` carries metadata only (id, path, format, openedAt); the engine layer owns POI workbook state internally, keyed by `HandleId`. Future better: once engine layer is in place, document this contract explicitly in the README "Architecture" section and add ArchUnit tests enforcing it (already listed under §14.6).
- **`sheet.create` silently clamps out-of-range `index` values instead of rejecting them.** (Phase 6 manual testing, `SheetCreateTool`) `index: -1` inserts the new sheet at position 0; `index: 999` (greater than the current sheet count) appends at the end. POI's `setSheetOrder` accepts and normalises these values without complaint, and the tool currently passes them through. Chose: accept as defensive v1 behaviour — a slightly-wrong index is usually the agent's off-by-one mistake and clamping to the nearest valid position is more useful than a hard error. Future better: if strictness matters for a caller, either validate the range up front and return a new `SHEET_INDEX_INVALID` code, or gate the strict behaviour behind an env var / tool-input flag. Low priority; flag via tool description if the behaviour ever surprises an agent.
- **Resolved (post-v1 review fix-up): `sheet.delete` now rejects the last remaining sheet with `SHEET_LAST_REMAINING`.** (Phase 6 manual testing → review fix-up Batch B2) Originally v1 left the invariant to the agent ("always keep at least one sheet") with only a tool-description warning. The post-v1 review elevated this to must-fix because save would happily produce a zero-sheet workbook that Excel refuses to open. `SheetDeleteTool.execute` now pre-checks `engine.listSheets(id).size() == 1` and throws the new `SHEET_LAST_REMAINING` code; tool description and implementation-plan §8.2/§10 sheet.delete entry updated accordingly. Regression: `smoke/phase-regression-sheet-last-remaining.sh`. Auto-seed-replacement remains an alternative for a future variant that wants delete-and-replace semantics.
- **`sheet.rename` treats same-name rename as a silent no-op, and consequently accepts case-only renames too.** (Phase 6 manual testing, `SheetRenameTool`) `rename "X" → "X"` succeeds without error (Excel itself tolerates this — the rename dialog just closes); `rename "X" → "x"` also succeeds because the case-insensitive duplicate pre-check skips when the target sheet IS the source sheet. Net effect: case-only renames are a real and intentional feature even though the case-insensitive lookup rule (Phase-2 deferral) would otherwise block setting a "duplicate" name that differs only in case. This is the correct design: the only case-only collisions we need to block are ones involving *different* sheets. Chose: document as v1 behaviour; no change. Future consideration: if an agent relies on case-only renames as a supported operation, promote it from "incidentally works" to a documented guarantee in the tool description.
- **`named_range.get` rejects anything that isn't a simple rectangular area — formula expressions, multi-sheet 3D refs, and sheet-less references all surface as `NAMED_RANGE_NOT_FOUND`.** (Phase 8, `XssfInMemoryEngine.readNamedRange`) Defined names whose `refersToFormula()` POI can't turn into a single `AreaReference` (e.g. `=SUM(A1:A10)`, `=Sheet1:Sheet3!A1`, bare `=A1` without a sheet qualifier) are not resolvable to a range-read call without a full expression evaluator. Chose: surface as `NAMED_RANGE_NOT_FOUND` with the original `refers_to` string in `details` so the agent can see what's going on — the v1 error enum has no dedicated `NAMED_RANGE_INVALID` code and `named_range.list` already exposes the full defined-name inventory including oddball refers-to values. Future better: either extend the enum with `NAMED_RANGE_NOT_A_RANGE` (clearer code) or fall through to a full expression evaluator that rewrites the expression into a concrete A1 range (handles `=Sheet1!A1:B2` and similar common cases without triggering the reject path).
- **`named_range.list` returns sheet-scoped entries with `scope:"sheet"` including the sheet name, but there can still be multiple names with identical `name` scoped to different sheets and `workbook.getName(name)` returns only the first match.** (Phase 8, `XssfInMemoryEngine.readNamedRange`) Excel lets two sheets each own a sheet-scoped `Total` alongside a workbook-scoped `Total`. `named_range.get` uses `workbook.getName(name)` which returns "first match" per POI's contract — so an agent asking for `Total` will get one of potentially several. Duplicate-name scoping is rare but real, and the v1 tool has no way to disambiguate scope. Future better: extend `named_range.get` inputs with an optional `scope: "workbook" | "<sheet_name>"` discriminator, and resolve via `workbook.getNames(name)` + filter; or return `NAMED_RANGE_AMBIGUOUS` with the list of candidates when multiple names share the identifier.
- **Table-name lookup is case-insensitive across the whole workbook.** (Phase 8, `XssfInMemoryEngine.readTable`) Excel treats table names as case-insensitive and enforces workbook-wide uniqueness, so iterating every sheet and matching case-insensitively is correct. We do not short-circuit when POI's XSSF-table metadata is missing a `name` — we silently skip those entries. Future better: surface malformed-name table entries in the `details` of `TABLE_NOT_FOUND` so operators can distinguish "no such table" from "table present but nameless".
- **Resolved Phase 10: `.xlsm` smoke fixture now committed at `src/test/resources/fixtures/vba-hello.xlsm`.** (Phase 9 deferral, closed Phase 10) Originally the Phase 9 smoke built the fixture at test time by extracting `xl/vbaProject.bin` from whatever `.xlsm` `PHASE9_VBA_SOURCE` (or a hardcoded dev-host fallback) resolved to. Phase 10 built the fixture once via `BuildVbaFixture` (copy `vbaProject.bin` out of `test-data/vba-test.xlsm`, embed in a fresh `XSSFWorkbook` with one Sheet1 via `setVBAProject(InputStream)`, write to disk), committed the ~6.3 KB result, and rewrote `smoke/phase9-smoke.sh` to require the committed fixture unconditionally — no more env-var, no hardcoded host path. Provenance is recorded in `src/test/resources/fixtures/README.md`. Regeneration is a one-shot rebuild against any macro-enabled source workbook.
- **VBA extraction reads from the workbook's on-disk source file, not from the in-memory workbook.** (Phase 9, `PoiVbaExtractor`) POI preserves `vbaProject.bin` as an opaque OPC part on round-trip but has no in-memory VBA parse/edit API — `VBAMacroReader` takes a `File`, `POIFSFileSystem`, or `InputStream`. We take the simplest path: open the workbook's `sourcePath` as a `File` and run `VBAMacroReader` against it. Consequence: a workbook loaded from a `.xlsm` that is later modified externally while our handle is open, then queried for VBA, will return the on-disk VBA (which is still accurate — we don't mutate VBA in-memory), but if the external writer overwrote the file after our `workbook.open`, VBA extraction races with the write. This is analogous to the Phase-4 data-loss concern (fixed by loading from `InputStream`) but narrower since VBA is never rewritten by POI on save. Future better: extract VBA from the live `OPCPackage` held in memory via `getPart(/xl/vbaProject.bin)` → `InputStream` → `POIFSFileSystem` → `VBAMacroReader`; removes the disk dependency and makes the call work for `workbook.create` paths too (though those never have VBA).
- **VBA module type set is `{module, class, document}` — no `form`.** (Phase 9, `PoiVbaExtractor.wireType`) Plan §7.3 lists `module | class | document | form`, but POI's `Module.ModuleType` enum has only three values (Document, Module, Class) because UserForm extraction is known-incomplete in every Java and Python tool (see future-work §4). Callers that need form metadata will see the form's UserForm module surface as a `document` or not at all, depending on how Excel serialised it. Future better: document the limitation in the tool description (currently done), and if UserForm extraction ever lands in POI, revisit the wire-value mapping.
- **`VBA_NOT_PRESENT` covers four distinct conditions with one code: no source path, source-path file missing, OPC part absent, and VBA binary unreadable.** (Phase 9, `PoiVbaExtractor.readModulesOrFail`) The plan's error set has one code for "no VBA here" and we collapse all four into it — each with a distinct message/details combo so operators can still tell them apart in logs. Future better: split into `VBA_NOT_PRESENT` (no macros, honest absence) vs. a new `VBA_UNREADABLE` (file exists, has a vbaProject.bin, but POI failed to parse it — either corrupt or a format POI can't read); the split matters if an agent wants to retry-after-recovery vs. accept-absence.
- **Fixed: `named_range.list` and `listTables` returned raw POI reference strings instead of the plan §7.3 split `{sheet, range}` shape.** (Post-Phase-9 manual testing, `XssfInMemoryEngine.listNamedRanges` and `listTables`) Named-range entries surfaced as `{"name":"Total","sheet":null,"range":"Data!$C$6","scope":"workbook"}` and table entries as `{"name":"tblSales","sheet":"Data","range":"Data!$A$1:$C$4"}` — the reference's sheet prefix and absolute markers were baked into the `range` field, duplicating the sheet name in table entries and missing it entirely for workbook-scoped named ranges. Root cause: `listNamedRanges` passed `n.getRefersToFormula()` through unchanged and set `sheet` from `n.getSheetIndex()` (the *scope* sheet, not the range's sheet); `listTables` passed `t.getArea().formatAsString()` (POI's "full" formatter, with prefix and `$`). Fix: both methods now parse the underlying `AreaReference` and use the existing private `formatAreaA1` helper to render a bare A1 body, taking `sheet` from `area.getFirstCell().getSheetName()`. Named ranges whose `refersTo` doesn't parse as a rectangular `AreaReference` (formula expressions, 3D refs, bare A1 without sheet) now surface with `sheet` and `range` both `null` — the list call must tolerate odd entries rather than failing the whole inventory. Same underlying `listNamedRanges` / `listTables` feed `workbook.metadata`, so that tool is fixed as a side effect. Regression: `smoke/phase8-smoke.sh` asserts `{sheet: "Data", range: "C6"}` (and the two other named ranges) plus `{sheet: "Data", range: "A1:C4"}` for `tblSales`, with an explicit `"!" not in range and "$" not in range` check; same assertions re-run against `workbook.metadata`.
- **Fixed: `vba.get_module` on a missing module name wedged the MCP Inspector's stdio transport.** (Post-Phase-9 manual testing, `PoiVbaExtractor.getModule` + `ToolRegistry.errorResult`) Calling `vba.get_module` with a non-matching name on a workbook that DID have VBA produced the correct `VBA_MODULE_NOT_FOUND` envelope in the Python smoke but dropped the Inspector's connection with "Not connected / transports removed" — implying the error-envelope serialisation path could throw past the SDK boundary under some clients. Fix applied in two places: (1) `PoiVbaExtractor.getModule` now constructs `details` with `List.copyOf(modules.keySet())` instead of `new ArrayList<>(modules.keySet())`, guaranteeing a fully-serialisable immutable `List<String>` for Jackson; (2) `ToolRegistry.errorResult` already had a try/catch around envelope serialisation, but its fallback text lacked a `details:{}` field and its `structuredContent` slot was empty — both are now populated with `{code:"INTERNAL_ERROR", message:"error-response serialization failed", details:{}}` and the failure is logged at ERROR so an opaque transport drop is never silent. Regression: `smoke/phase9-smoke.sh` now follows the `VBA_MODULE_NOT_FOUND` assertion with an immediate `tools/list` call (fresh request id) — if stdout had been corrupted or the Java thread died, that second call would hang or EOF instead of returning the registered tool set.
- **Reverted (follow-up to Bug 2 above): the hypothesis that `structuredContent` + `isError: true` on the same `CallToolResult` was what killed the MCP Inspector's transport turned out to be wrong.** (Post-Phase-9 manual testing round 2, `ToolRegistry.errorResult`) A speculative fix removed the `.structuredContent(...)` call from both branches of `errorResult` on the theory that the Inspector's SSE proxy was dropping the transport because of that field+flag combination. Manual retest against the rebuilt image showed the Inspector **still** crashes with "Command not found, transports removed" on `vba.get_module` with a non-existent module name — so the removal didn't address the real root cause, and continuing to ship it would have reduced error-response information for well-behaved clients without benefit. The change has been reverted: `errorResult` once again calls `.structuredContent(jsonMapper.convertValue(env, Map.class))` in the main branch and a hand-built `Map` echo in the fallback branch. The defensive `try/catch` wrapper around serialisation (added in the original Bug 2 round) is kept — it's an unrelated improvement that costs nothing and stops an unserialisable details map from taking down the transport. Evidence the Java server itself is correct: `smoke/phase9-smoke.sh` includes a transport-survives-error regression (issues `tools/list` immediately after a `VBA_MODULE_NOT_FOUND` error and asserts the registered tool set comes back cleanly) — it passes against the current code. The Inspector crash is therefore **suspected to be a client-side issue** in the `@modelcontextprotocol/inspector` SSE proxy when a tool-error response crosses a specific size / shape threshold; reproducing it against a second independent MCP client (e.g. Claude Desktop, a custom TS client, or the Python SDK) would confirm. Future better: once a minimal reproducer outside the Inspector is in hand, open an upstream issue; in the meantime, advise operators to use the raw-stdio smoke test or a different client when investigating error-path behaviour. **This item stays open — the Inspector interop bug has NOT been fixed, only ruled out as a server bug.**
- **Fixed: VBA tools on a plain `.xlsx` (no `vbaProject.bin`) returned `INTERNAL_ERROR` instead of `VBA_NOT_PRESENT`.** (Post-Phase-9 manual testing, `PoiVbaExtractor.readModulesOrFail`) Opening a plain `.xlsx` via `workbook.open` and then calling `vba.list_modules` produced `{"code":"INTERNAL_ERROR","message":"IllegalArgumentException: No VBA project found"}` — a clear plan §9.7 contract violation. Root cause: POI 5.5.x's `VBAMacroReader.readMacroModules()` throws `java.lang.IllegalArgumentException("No VBA project found")` when the file has no `vbaProject.bin`; the existing `catch (IOException ex)` in `PoiVbaExtractor.readModulesOrFail` didn't cover it, so the exception bubbled up and was mapped to `INTERNAL_ERROR` by the dispatcher's catch-all. The adjacent code comment claiming "VBAMacroReader returns an empty map" was stale — confirmed via live probing. Fix: widen the catch to `catch (IOException | IllegalArgumentException ex)` and map both to `VBA_NOT_PRESENT` with the existing message structure (POI's message passes through `details.exception`). The `modules.isEmpty()` defensive branch is kept as forward compatibility against a future POI that switches back to empty-map semantics; comment rewritten to explain the actual POI 5.5.x behaviour. Regression: `smoke/phase9-smoke.sh` tier-B — `workbook.create` → `workbook.save` to a tmp `.xlsx` → `workbook.close` → `workbook.open` the saved file → `vba.list_modules` must return `VBA_NOT_PRESENT`, not `INTERNAL_ERROR`. This is the exact path the reporter hit; the earlier tier-A `workbook.create` path short-circuited in the null-source-path check and couldn't exercise the bug.
- **Fixed: `workbook.recalculate` returned stale formula values (or threw `FORMULA_EVAL_FAILED`) after any structural or formula mutation because the cached POI `FormulaEvaluator` was never invalidated.** (`PoiFormulaEvaluation.evaluateAll`, post-Phase-10 follow-up) `XssfInMemoryEngine` caches one `FormulaEvaluator` per handle (`Map<HandleId, FormulaEvaluator>`, ~line 65) for reuse across recalc calls — correct for perf, but POI's `FormulaEvaluator` maintains an internal cell-address-keyed result cache that survives `sheet.shiftRows`, `sheet.shiftColumns`, and in-place formula rewrites via `cell.setCellFormula`. `wb.setForceFormulaRecalculation(true)` only affects Excel-side recalc on next open, not POI's own cache. Reproduced sequence: create workbook → `range.set` values in A1:B3 with `=SUM(B1:B3)` in B4 → recalc (correct) → `sheet.insert_rows` at row 1 count 1 (POI rewrites the formula text to `=SUM(B2:B4)` and shifts B4 to B5) → recalc returns the pre-shift cached sum at the new B5 address (or, when the shift leaves the cache inconsistent with POI's parser state, throws `IllegalStateException` surfaced as `FORMULA_EVAL_FAILED`). Cross-formula dependencies inherit the staleness even when their own formula text is freshly written. Fix: `evaluator.clearAllCachedResultValues();` as the first line of `PoiFormulaEvaluation.evaluateAll`, before the existing `wb.setForceFormulaRecalculation(true)` call. One-line change, negligible perf cost (recalc is user-triggered, not hot-path). Handles all mutation paths (row/col shifts + in-place `range.set` on referenced formulas) with a single invalidation point rather than tracking mutations per tool. Regression: `smoke/phase-regression-recalc-after-row-shift.sh` reproduces the exact sequence and asserts the post-shift recalc returns the fresh sum, not the pre-shift cached value; a revert-and-rerun during development confirmed the smoke fails without the extra invalidation line (throws `FORMULA_EVAL_FAILED`). Future better: consider scoping the invalidation to just the cells POI's shift touched (via `notifyUpdateCell`) if large-workbook recalc latency ever becomes an issue; not worth the complexity for v1.
- **POI quotes sheet references in rewritten formulas after `sheet.rename`, even when quoting isn't strictly required.** (Phase 6 manual testing, observed via `range.get` on a cross-sheet formula after rename) A formula `=X!A1` becomes `='Y'!A1` (single-quoted) after `rename X → Y`, even for plain alphanumeric names where Excel's formula grammar does not require quoting. POI applies proactive quoting as a safety margin when rewriting sheet references during rename — both forms parse and evaluate identically in Excel, and agents reading formulas back should tolerate either. Operator reported that manually stripping the quotes in Excel produced a formula-parse error in one test; reproducibility unconfirmed and likely the result of unrelated edit artefacts rather than the quoting itself. Chose: accept POI's proactive quoting as v1 behaviour; agents consuming formula strings should treat `'X'!A1` and `X!A1` as equivalent. Future better: if cleaner formula strings become important (e.g. for deterministic diffs across rename operations), post-process the rewritten formulas to strip quotes around names that match POI's "no quoting required" identifier regex.
